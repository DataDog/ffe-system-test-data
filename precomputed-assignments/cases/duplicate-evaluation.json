{
  "name": "duplicate-evaluation",
  "description": "The same flag is evaluated twice. Validates that the SDK returns consistent values and that exposure dedup produces only one exposure event.",
  "context": {
    "targetingKey": "dedup-user",
    "attributes": {}
  },
  "response": {
    "data": {
      "attributes": {
        "flags": {
          "fixture.dedup.flag": {
            "allocationKey": "alloc-dedup",
            "variationKey": "variant-a",
            "variationType": "string",
            "variationValue": "alpha",
            "reason": "TARGETING_MATCH",
            "doLog": true
          }
        }
      }
    }
  },
  "evaluations": [
    {
      "flag": "fixture.dedup.flag",
      "variationType": "string",
      "defaultValue": "default",
      "result": {
        "value": "alpha",
        "reason": "TARGETING_MATCH",
        "errorCode": null,
        "variantKey": "variant-a"
      }
    },
    {
      "flag": "fixture.dedup.flag",
      "variationType": "string",
      "defaultValue": "default",
      "result": {
        "value": "alpha",
        "reason": "TARGETING_MATCH",
        "errorCode": null,
        "variantKey": "variant-a"
      }
    }
  ],
  "expectedEmissions": {
    "exposures": 1,
    "flagevaluationRequests": 1,
    "flagevaluationEvents": 2
  },
  "skipForSdks": [
    "web"
  ],
  "skipReason": "Web browser SDK's FlagEvaluationAggregator deduplicates: evaluating the same flag twice produces one event with evaluation_count=2, not two events. Fixture expects flagevaluationEvents=2 which is correct for non-aggregating platforms."
}
